/* ============================================================
   BLCM — Archivo de configuración
   Edita este archivo y haz git push para activar o desactivar
   audiciones sin tocar el resto del código.
============================================================ */
const BLCM_CONFIG = {
    audiciones: {
        global: true,   // ← true = audiciones abiertas | false = cerradas
        secciones: {
            liras:       true,
            guiros:      true,
            cajas:       true,
            bombos:      true,
            tenores:     true,
            principales: true,
            colorguard:  true,
            folclore:    true
        }
    }
};
